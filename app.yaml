runtime: nodejs16
env: standard

env_variables:
  PORT: "5000"
  SCRATCH_API_BASE: "https://api.scratch.mit.edu"
  ADMINS: "Seita-2015-0216,hikinikuunnma"

automatic_scaling:
  min_instances: 1
  max_instances: 10
